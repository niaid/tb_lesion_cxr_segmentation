name: tb_segmentation
channels:
  - conda-forge
  - defaults
dependencies:
  - postgresql
  - pip:
      - simpleitk
      - pandas
      - scikit-learn
      - pydicom
      - monai
      - numpy
      - ultralytics
      - nnunetv2
      - optuna

